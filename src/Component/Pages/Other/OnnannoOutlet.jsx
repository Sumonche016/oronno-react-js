import React from 'react';
import { Outlet } from 'react-router-dom';
const OnnannoOutlet = () => {
    return (
        <div>
            <Outlet></Outlet>
        </div>
    );
};

export default OnnannoOutlet;